<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shf.mybatisplus.mapper.UserMapper">
    <sql id="Base_Column_List">
        uid,
        username,
        age,
        email
    </sql>

    <resultMap id="BaseResultMap" type="com.shf.mybatisplus.entity.User">
        <result column="uid" property="id"/>
        <result column="username" property="name"/>
        <result column="age" property="age"/>
        <result column="email" property="email"/>
    </resultMap>

<!--auto generated by MybatisCodeHelper on 2022-07-22-->
    <select id="selectAllByName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user1
        where username=#{name}
    </select>

    <select id="selectPageByPage" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from user1
        where age>#{age}
    </select>
</mapper>